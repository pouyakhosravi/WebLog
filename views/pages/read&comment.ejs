<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ("../partial/headTag.ejs") -%>
    <!-- local css -->
    <link rel="stylesheet" href="/CSS/read&comment.css">
    <title>مقاله <%= article.author.sex + " " + article.author.lastName%> </title>
</head>

<body>

    <!-- show info panel -->
    <div class="card mb-3" style="max-width: 540px;">
        <div class="row no-gutters">
            <div class="col-md-4">
                <img src="/Avatars/<%= article.author.avatar %>" class="card-img" alt="...">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title"><%= article.author.firstName +" " + article.author.lastName%></h5>
                    <p class="card-text"><%= article.author.sex %> </p>
                    <p class="card-text">تاریخ ثبت این مقاله:</p>
                    <p class="card-text"><small class="text-muted"><%= article.createdAt %> </small></p>
                </div>
            </div>
        </div>
    </div>

    <div class="title"><%= article.description %> </div>

    <div class="text"><%- article.text %> </div>

    <!-- bootstarp for commit comment -->
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal"data-whatever="@mdo">ثبت نظر</button>

    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
        aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">نظر جدید</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form>
                        <div class="form-group">
                            <label for="recipient-name" class="col-form-label">نام:</label>
                            <input type="text" class="form-control" id="recipient-name" required>
                        </div>
                        <div class="form-group">
                            <label for="message-text" class="col-form-label">پیام:</label>
                            <textarea class="form-control" id="message-text"></textarea>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">لغو</button>
                    <button article-id="<%= article._id %>" id="submitComment" class="btn btn-primary">ثبت نهایی نظر</button>
                </div>
            </div>
        </div>
    </div>

    <h1 class="head" >لیست نظرات</h1>
    <div class="commentsList">
        <% if (comments.length) { %>
            <% for( let i = 0; i < comments.length; i++ ) { %>
                <%- include ("../partial/comments-panel.ejs", {comment: comments[i]}) -%> 
            <% } %>
        <% } else { %>
            <h1 class="nothing">هیچ نظری وجود ندارد.</h1>
        <% } %> 
    </div>

    <%- include ("../partial/body-script.ejs") -%>
    <!-- local file -->
    <script src="/JS/read&comment.js"></script>
</body>

</html>